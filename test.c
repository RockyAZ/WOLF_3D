#include <stdio.h>
#include <math.h>
# define CUBE 64
int main()
{
	int i = 3;
	float f  = (float)i / (float)CUBE;
	printf("%f\n", cos(60));
	printf("%f\n", cos((60 * 2 * M_PI) / 360));
	
	return (0);
}
